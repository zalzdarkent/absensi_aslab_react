@startuml Pendataan Aset dan Bahan
!theme plain
skinparam ConditionEndStyle hline
skinparam conditionStyle diamond

title Activity Diagram - Pendataan Aset dan Bahan

|Admin|
|Aslab|
|Sistem|

|Admin|
start
:Masuk Halaman\nPendataan Aset;

|Aslab|
:Masuk Halaman\nPendataan Bahan;

|Admin|
:Input Data Aset/Bahan\n(nama, jenis, stok, status);

|Sistem|
:Tampilkan Form\nInput Data;

|Admin|
if (Upload Gambar?) then (ya)
  :Upload File Gambar;
else (tidak)
endif

|Sistem|
if (Ada Gambar?) then (tidak)
  :Gunakan Gambar Default;
else (ya)
  :Proses Upload Gambar;
endif

:Validasi Input Data;

if (Data Valid?) then (tidak)
  :Tampilkan Error\nValidasi;
  stop
else (ya)
  :Generate Kode Otomatis;
  :Simpan ke Database\n& Storage;
  :Tampilkan Pesan\nSukses;
  :Tampilkan Daftar\nAset/Bahan;
  stop
endif

@enduml
