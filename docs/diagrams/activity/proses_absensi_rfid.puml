@startuml Proses Absensi RFID
!theme plain
skinparam ConditionEndStyle hline

title Activity Diagram - Proses Absensi RFID

|User (Aslab & Admin)|
start

if (Membawa RFID Card?) then (Ya)
  :Scan RFID Card ke Reader;
  
  |Sistem Absensi|
  :Validasi ID Card & User;
  
  if (RFID Valid?) then (Tidak)
    :Tampilkan Pesan Error\n"RFID Tidak Terdaftar";
    detach
  else (Ya)
    :Cek Status Absensi Hari Ini\n(Database);
  endif
  
  if (Sudah Check-in?) then (Belum)
    :Simpan Data Check-in;
    :Tampilkan Pesan Sukses\n"Selamat Datang";
    detach
  else (Sudah)
    if (Sudah Check-out?) then (Belum)
      :Simpan Data Check-out;
      :Tampilkan Pesan Sukses\n"Sampai Jumpa";
      detach
    else (Sudah)
      :Tampilkan Pesan Error\n"Anda Sudah Absen Lengkap";
      detach
    endif
  endif

else (Tidak - Manual)
  |User (Aslab & Admin)|
  :Lapor ke Admin untuk\nAbsen Manual;
  :Admin Input Data Absensi\nsecara Manual;
  
  |Sistem Absensi|
  :Proses Simpan Data Manual;
  :Tampilkan Status\nSukses/Gagal;
  stop

endif

@enduml