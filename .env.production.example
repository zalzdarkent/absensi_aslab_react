# ========================================
# PRODUCTION .env Configuration Checklist
# ========================================

# Application Environment
APP_ENV=production
APP_DEBUG=false
APP_URL=https://yourdomain.com

# Telegram Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_BOT_USERNAME=@YourBot_bot

# Database (sesuaikan dengan production database)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=absensi_aslab_production
DB_USERNAME=production_user
DB_PASSWORD=secure_password

# Cache & Session (untuk production, gunakan Redis jika memungkinkan)
CACHE_STORE=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis

# SSL & Security
# Pastikan APP_URL menggunakan HTTPS
# SSL certificate harus valid untuk domain

# ========================================
# IMPORTANT NOTES FOR PRODUCTION:
# ========================================

# 1. APP_ENV=production → SSL verification ENABLED untuk Telegram API
# 2. APP_DEBUG=false → Tidak ada debug info di log
# 3. HTTPS required untuk Telegram webhook
# 4. Database credentials harus aman
# 5. Use Redis untuk better performance di cache/queue

# ========================================
# POST-DEPLOYMENT STEPS:
# ========================================

# 1. Set telegram webhook:
#    php set_webhook_production.php https://yourdomain.com
#
# 2. Test bot commands:
#    /start, /status, /chatid, /jadwal
#
# 3. Test attendance notifications:
#    Coba RFID scan dan pastikan telegram notification masuk
#
# 4. Monitor logs:
#    tail -f storage/logs/laravel.log

# ========================================
